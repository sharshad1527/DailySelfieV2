"""
core/app_info.py

Single source of truth for application metadata.

The About page, installers and .desktop templates all read the
user-visible version string from here so they stay in sync.
"""

APP_VERSION = "1.0"
